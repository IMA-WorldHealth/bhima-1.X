angular.module('kpk.controllers')
.controller('report.stock_count', [
  '$scope',
  'appstate',
  'connect',
  'messenger',
  function ($scope, connect, validate, messenger) {
    var dependencies = {};
    var session = $scope.session = {};

  
  }
]);

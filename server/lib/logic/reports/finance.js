/*Placeholder file - not yet used*/
module.exports.path = "finance"

module.exports = (function() { 

  function buildReport() { 
    console.log('[buildReport]');
  }

  return {
    'buildReport' : buildReport
  };
});